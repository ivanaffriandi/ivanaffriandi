# Rspamd DKIM Signing Configuration for ivanaffriandi.com

path = "/var/lib/rspamd/dkim/$domain.$selector.key";
selector = "202608mail";

allow_envfrom_mismatch = true;
allow_hdrfrom_mismatch = false;
allow_hdrfrom_mismatch_sign_networks = true;
allow_username_mismatch = false;

use_domain = "header";
sign_authenticated = true;
sign_inbound = false;

# RSA 2048-bit / Ed25519 selectors
domain {
    ivanaffriandi.com {
        path = "/var/lib/rspamd/dkim/ivanaffriandi.com.202608mail.key";
        selector = "202608mail";
    }
}
